//Verifica se o nome informado é valido ou não, recebe umvetor de char.
int verificaNome(char x[]); 

//Verifica se um char informado é Letra.
int validaLetra(char c);

//Valida cpf por tamanho e verificando se é so numeros.
int validaCpf(char x[]);

//Valida rg por tamanho e verificando se é so numeros.
int validaRg(char x[]);

//Verifica se umchar informado é numero.
int validaNumero(char x);

//Verifica se um ano qualquer é bissexto.
int bissexto(int);

//Valida uma data recebendo ters int's, int/int/int.
int dataValida(int, int , int);
